/// <summary>
/// Defines all audio identifiers used by the AudioAlchemistPro system.
/// Each value corresponds to a sound configured in the audio database.
/// 
/// Usage:
///     AudioAlchemistPro.Instance.Play(AudioID.Laser);
///     AudioAlchemistPro.Instance.Stop(AudioID.Engine);
/// </summary>
public enum AudioID
{
    None,
    Jump,
}